spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/pizzariadankicode?createDatabaseIfNotExist=true
    username: root
    password: 1234

  jpa:
    show-sql: true

server:
  error:
    include-stacktrace: NEVER
